import React from 'react';
import {connect} from "react-redux";


// The add row dispatcher choose the
// action we dispatch.
// If the column in question has content
// then first we must encapsulate the
// content. Then we add another row
const AddRowDispatcher = ({}) => {

};


